primitives:
  neutral:
    light: "#FFFFFF"
    base:  "#787880"
    dark:  "#101012"
    ic:
      light: "#FFFFFF"
      base:  "#72727A"
      dark:  "#000000"
    # Optional curve tuning.  Omit to use defaults.
    # curve:
    #   lightness_ease: 1.7
    #   hue_ease: 0.6
    #   chroma_peak: 0.35

output:
  scss: "dist/tokens.scss"
  json: "dist/tokens.json"
